//! Cairo public statement construction and transcript inputs.

pub const public_data = @import("public_data.zig");
pub const public_logup = @import("public_logup.zig");

test {
    _ = public_data;
    _ = public_logup;
}
