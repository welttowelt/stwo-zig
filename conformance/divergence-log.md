# Upstream divergence ledger

This is the operative ledger for intentional differences between the Zig
implementation and pinned external authorities or historical references. A
difference remains a release blocker unless its row explicitly says otherwise.
Removing a difference must remove the row in the same commit.

Some rows are architectural disclosures rather than temporary debts: their
absence is itself a gate failure, because deleting them would hide a permanent
divergence or an oracle limitation. Those rows are listed in
`REQUIRED_ARCHITECTURAL_DIVERGENCES` in
`scripts/riscv_release_gate_lib/contract.py`, and their conditions are read by
`python3 scripts/check_riscv_release_contract.py --all`; a row's status text and
the code that enforces it must be changed together.

## Active divergences

| Lane | Boundary | Current Zig behavior | Pinned-oracle behavior | Release status |
| --- | --- | --- | --- | --- |
| RISC-V | PCS geometry | Uses the repository's lifted PCS and folded query points. | Stark-V uses its upstream commitment/evaluation geometry. | Allowed only with the committed composition self-check and proof mutation coverage; not a semantic waiver. |
| RISC-V | Interaction transcript | Relative to the historical Stark-V prefix, Zig mixes a domain-separated shard manifest before the fixed schema-v3 10-bit PoW nonce, twelve relation pairs, interaction claim/root, and downstream PCS. The production prover and verifier expose caller-owned channels; their exact byte-event traces agree, and independent CLI processes bind the final channel digest plus draw counter, implementation commit/dirty state, and executable SHA-256 in strict receipts. | Stark-V has no shard-manifest extension and its generated constant drops PoW to 1 bit in debug builds. | Allowed only with the committed full-path event-symmetry test, current protocol event-vector pins, fixed release security parameters, transcript-state draw-count regression, and a fresh separate-process receipt from the exact candidate executable. Stark-V prefix parity is historical compatibility information, not an admission premise; byte-identical proof-wire parity is not claimed after the documented extension. |
| RISC-V | RV32IM decode boundary | Rejects `FENCE.I` (`0x0000100f`) before witness construction because Zifencei is outside `rv32im-zkvm-v1`. | Pinned Sail retires the word even with `extensions.Zifencei.supported=false` and reports the configured ISA as `rv32im`. | Allowed only with the machine-profile record of the narrowing and the live negative gate that requires Zig rejection plus the pinned Sail disposition. This is a conservative narrowing: no admitted instruction semantics are overridden. |
| RISC-V | Opcode AIR constraint and lookup layout | Closes eight demonstrated omissions in the pinned AIR. (1) Every read-only access binds `next == previous`. (2) `SB`/`SH` preserve unmarked destination bytes. (3) `AUIPC` pins the U-immediate decomposition. (4) JALR keeps its 32-column legacy prefix and appends nine columns for its source, signed immediate, bit 0, wraparound, and bounded `target / 4` split; its relation vector is 18 entries / 9 batches instead of 12 / 6. (5) DIV rows add two divisor-byte requests and one signed-only quotient-sign request, for 25 rather than 22 entries; direct equations pin unsigned signs, preserving high-bit DIVU/REMU completeness. (6) Signed loads and right shifts bind their sign witnesses. (7) Shift carries use `(carry, bit_multiplier - 1 - carry)` in `range_check_8_8`, raising `shifts_reg` to 20 entries / 10 batches and `shifts_imm` to 16 / 8. (8) Every live register or RW-memory access derives an ordered subclock `4 * (instruction_clock - 1) + ordinal + 1`; residue zero is reserved and `range_check_20` receives `current - previous - 1`. Clock-update predecessors expand from low20/high4 to low20/high6, with `(high6, 4 * high6)` proving the `2^26` bound. No opcode column is added for this clock fix, but memory tuples, range tuples, public final-access clocks, and the clock-update lookup domain intentionally change. Statement admission additionally bounds every same-sign memory-relation source below M31 and requires `2 * merkle_rows + program_rows + memory_rows + 3 < p`, so malicious cross-source Merkle tuple collisions cannot wrap a coefficient. | Pinned Stark-V admits unconsumed source values, free partial-store bytes and sign witnesses, the AUIPC `p + 2` alias, an unbound JALR target decomposition, non-byte DIV divisors, negative shift carries, and same-instruction accesses at one clock with a zero-admitting raw gap. The last pair permits an aliased read to hide an arbitrary operand in a same-tuple/same-clock self-loop. | Allowed only with current Sail semantics, complete family coverage, row-rigidity tests, relation-domain closure, infrastructure certificates, and proof-level adversarial tests remaining green. `per_family_witness_rows`, `relation_tuples`, and `relation_sums` differ from Stark-V by design; the archived CP-11 parser cannot authorize release and no divergence shape may be added for that purpose. Coverage is canonical and complete at family granularity: `FAMILY_COVERAGE` names all 17 families in production order, requires one pre-ingestion forgery plus a paired honest prove/verify/pinned-Sail case for each, and compile-fails on drift. The all-family rigidity suite and the 292-case Sail operand sweep remain bounded evidence rather than universal refinement. Focused row checks and counterexamples attribute the eight fixes, including exact shift-carry entries and rejection of the former same-clock alias; bus-overlapping forgeries may still be rejected by more than one relation, so their end-to-end tests assert rejection stage rather than unique relation attribution. The coefficient guards are source-bound by the infrastructure certificates and boundary tests. The checkpoint, conditional cross-row lemmas, and exact limitations are indexed in `soundness/ROADMAP.md` and `soundness/SAIL_AIR_COMPOSITION.md`. |

## Closure requirements

Every release-blocking row requires prover/verifier integration, an adversarial
proof-level rejection test, and evidence against the applicable pinned
authority. Historical compatibility references are never acceptance
authorities. Internal Zig prove/verify consistency is necessary but cannot
close a row by itself.
