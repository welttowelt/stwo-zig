#!/bin/sh
set -eu

root=$(git rev-parse --show-toplevel)
cd "$root"

zig fmt --check build.zig src tools
python3 scripts/check_source_conformance.py
python3 -m unittest scripts.tests.test_source_conformance
