# Build outputs
.zig-cache/
zig-out/
target/
tools/stwo-vector-gen/target/
tools/stwo-interop-rs/target/

# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Metal pipeline archives
*.metallib.binarchive

# Judge run products (published signed to the judge-verdicts branch, never tracked)
judge-out/

# Regenerated run outputs. Evidence lives in the tracked history and archive
# directories under vectors/reports/; loose root-level products are ephemeral.
vectors/.fields.tmp.json
vectors/.bench*
vectors/.profile*
vectors/reports/*.json
vectors/reports/interop_artifacts/
vectors/reports/prove_checkpoints_artifacts/
