# Submissions

One directory per submission, created by `stwo-perf submit` and landed by PR.
Layout and note schema: [`../schema/submission.md`](../schema/submission.md).

Promoted submissions stay here forever — together with the ledger they are the
public record of what was tried, what was claimed, and what the judge measured.
